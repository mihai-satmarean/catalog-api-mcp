apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog-mcp-dev-storage
  namespace: catalog-mcp-dev
  labels:
    app: catalog-mcp-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
